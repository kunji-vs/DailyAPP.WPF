<UserControl x:Class="DailyAPP.WPF.Views.HomeUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DailyAPP.WPF.Views"
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--第一行-->
        <TextBlock Text="{Binding LoginInfo}" FontSize="22" VerticalAlignment="Center" Margin="15,10"/>
        <!--第二行-->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding StatPanelList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding BackgroundColor}" CornerRadius=" 5" Margin="10" Style="{StaticResource CardStyle}">
                        <Grid>
                            <StackPanel Orientation="Vertical" Margin="20,10">
                                <md:PackIcon Kind="{Binding Icon}" Width="30" Height="30"/>
                                <TextBlock Text="{Binding Name}" FontSize="15"  Margin="0,15"/>
                                <TextBlock Text="{Binding Result}" FontSize=" 40" FontWeight="Bold"  Margin="0,15"/>
                            </StackPanel>
                            <!--画背影-->
                            <Canvas ClipToBounds="True">
                                <Border Canvas.Top="10" Canvas.Right="-50" Width="120" Height="120" Background="#FFFFFF" CornerRadius="100" Opacity="0.1"/>
                                <Border Canvas.Top="80" Canvas.Right="-30" Width="120" Height="120" Background="#FFFFFF" CornerRadius="100" Opacity="0.1"/>
                            </Canvas>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Grid Grid.Row="2" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#C8E8FF" CornerRadius="5" Margin="10,0">
                <DockPanel Margin="10,0">
                    <DockPanel Margin="10,5" LastChildFill="False" DockPanel.Dock="Top">
                        <TextBlock Text="待办事项" FontWeight="Bold" FontSize="18"/>
                        <Button Width="30" Height="30" Style="{StaticResource MaterialDesignFloatingActionAccentButton}" DockPanel.Dock="Right" VerticalAlignment="Top"
                                Command="{Binding ShowAddWaitUC}">
                            <md:PackIcon Kind="Add"></md:PackIcon>
                        </Button>
                    </DockPanel>
                    <ListBox ItemsSource="{Binding WaitList}" HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel MaxHeight="80" LastChildFill="False">
                                    <ToggleButton IsChecked="{Binding Status}" DockPanel.Dock="Right"/>
                                    <StackPanel Orientation="Vertical" Margin="5">
                                        <TextBlock Text="{Binding Title}" Margin="0,10" Foreground="Black" FontSize="16" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Content}" Foreground="DarkGray"/>
                                    </StackPanel>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="#FFE2B1" CornerRadius="5" Margin="10,0">
                <DockPanel Margin="10,0">
                    <DockPanel Margin="10,5" LastChildFill="False" DockPanel.Dock="Top">
                        <TextBlock Text="备忘录" FontWeight="Bold" FontSize="18"/>
                        <Button Width="30" Height="30" Style="{StaticResource MaterialDesignFloatingActionAccentButton}" DockPanel.Dock="Right" VerticalAlignment="Top">
                            <md:PackIcon Kind="Add"></md:PackIcon>
                        </Button>
                    </DockPanel>
                    <ListBox ItemsSource="{Binding MemoList}" HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel MaxHeight="80" LastChildFill="False">
                                    <ToggleButton IsChecked="{Binding Status}" DockPanel.Dock="Right"/>
                                    <StackPanel Orientation="Vertical" Margin="5">
                                        <TextBlock Text="{Binding Title}" Margin="0,10" Foreground="Black" FontSize="16" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Content}" Foreground="DarkGray"/>
                                    </StackPanel>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

        </Grid>
    </Grid>
</UserControl>
