<UserControl x:Class="DailyAPP.WPF.Views.Dialogs.CustomMessageBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DailyAPP.WPF.Views.Dialogs"
             mc:Ignorable="d" Height="250" Width="350">
    <Grid Background="White" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Message}" FontSize="16" Margin="10" TextWrapping="Wrap"/>
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1" HorizontalAlignment="Right">
            <Button Content="确定" Height="30" Width="60" Margin="5,0" Command="{Binding ConfirmCommand}"/>
            <Button Content="取消" Height="30" Width="60" Margin="5,0" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
